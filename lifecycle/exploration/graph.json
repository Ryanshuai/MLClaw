{
  "_comment": "The experiment graph — one card per proposal, and the only order-of-work truth. Ported from the arch-transplant skill's `references/experiment-graph.md`, whose version lived in a markdown design doc and whose seven invariants said \"scan periodically\" with nothing scanning. `lifecycle/scripts/explore/graph.py check` is that scanner; this file is what it reads.\n\nA card is NOT a run. The arm that verifies a card is an ordinary training run in `stages/training/runs/`, cited here by `run_id` — same boundary `/data-curate` draws for a conversion and `adaptation` draws for a converter. Nothing in the explore stage executes.",

  "schema_version": 1,
  "stage": "exploration",

  "_comment_corpus": "‼️ The scope guard, and the reason this is a top-level field rather than a per-card one. Every `premise_share` on every card must have been measured on THIS corpus. arch-transplant's hardest rule — 「不许用另一个语料的数去杀一个提案」 — is unenforceable while the corpus is implicit, and it is the rule that cost the most: a share quoted from another corpus predicted 47% where this one measured 4.62%.\n\nSame shape as a run's `cfg.data.dataset_id` + `split_seed`, and for the same reason MLClaw refuses to compare metrics across a non-equivalent `scope`. Changing it invalidates every `premise_share` in the file — `graph.py check` says so rather than letting the numbers ride.",
  "corpus": {
    "dataset_id": null,
    "snapshot": null,
    "declared_at": null
  },

  "_comment_nodes": "One entry per proposal. `id` is a queue number, STABLE AND NEVER REUSED — a killed card's number is not recycled, because cross-references would then point at a different proposal.\n\nCard: {id, title, kind, state, premise, premise_share, criterion, guardrail, parent, depends_on, oracle_ceiling, kill_condition, tier, axes, run_id, result, verdict, killed_by, revive_if, history[]}\n\n‼️ A card missing any required field may not reach `ready`. That gate is the whole point of the draft state: an arm opened from an incomplete card produces a number nobody can adjudicate, and it costs GPU hours to find that out.",
  "nodes": [],

  "_comment_states": "Seven, and the split that matters is `filled` vs `closed` — 「有结果」不等于「有结论」. A card whose numbers are in but whose verdict is not is `filled`; treating it as `closed` propagates an unexplained number downstream as if it were a finding. The recorded instance: a mechanism criterion passed literally (the negatives did come back) while the verdict was 「机理验上了，而那个机理不值钱」, and that verdict needed another card's measurement before it could be reached.\n\ndraft    ⬜ 待补卡  — a one-line idea, schema incomplete\nblocked  🟨 待前置  — card complete, dependencies unmet\nready    🟩 就绪    — may open an arm\nrunning  🔵 在跑    — a run is declared; `run_id` is set\nfilled   🟪 已回填  — results on the card, VERDICT NOT YET REACHED\nclosed   ✅ 已裁决  — conclusion reached; may be another card's dependency\nkilled   ❌ 已杀    — with `killed_by` + `revive_if`; ALSO may be a dependency (\"stop waiting on this one\")",

  "_comment_provenance": "Who put this card on the queue: user | ai-suggested | ai-executed | user-revised. Borrowed from ARA (arXiv:2604.24658), whose Live Research Manager tags every entry the same four ways.\n\nLoad-bearing here because SKILL.md Stage 3 says this queue is 用户维护的执行队列, not the agent's notebook: the user drops in a one-line idea, the agent completes the card and executes it. Without the tag, a card the user demanded and a card the agent invented are the same row -- and the round's whole ordering is supposed to be the user's.\n\n‼️ `ai-suggested` never auto-upgrades. Only an explicit `set` moves it, because the entire value of the tag is that the agent's own confidence cannot earn it. Same rule as `/ask-human` refusing `verified` when nothing but the person confirmed it. The DISTRIBUTION is itself a quality signal, and `check` reports it.",

  "_comment_killed_by": "Four deaths, and their `revive_if` are differently shaped — writing them interchangeably is the same as not writing them.\n\nshare_too_small     the fault is real but rare here     → revive on a new corpus / new operating condition\nfaithful_but_inert  ported correctly, changed nothing   → revive on a changed metric or changed upstream\nwrong_mechanism     the proposal itself was wrong       → revive by measuring the effect DIRECTLY; the proxy condition need not hold\nunfaithful_port     ‼️ NOT a death — go back and fix it → no revive_if; it returns to `running`\n\n‼️ `revive_if` states a PROXY condition. A card revives the moment the effect is measured directly, whether or not the proxy fired — the recorded instance is a CDN card revived by direct measurement while neither of its two written revive_if conditions was ever met.",

  "_comment_tier": "T0–T4 from the verification-budget axes. ‼️ The tier travels with every number this card produces, forever. A `[T1 trend]` conclusion may not be cited next week as a `[T2 controlled]` one — soft numbers being promoted to hard conclusions is the single most common way this pipeline dies, and it is how a false noise floor entered the record once already.\n\nT0 not verified · T1 trend (short run/subset, noise floor NOT required) · T2 single controlled point (noise floor required) · T3 rides a full run · T4 cheap approximation, FOR PRICING ONLY — an approximation failing never refutes the original.",

  "_comment_axes": "The three that decide the tier: V verification cost · P prior · U project value. Each high|medium|low.\n\n‼️ V's main determinant is whether open-source code exists, not how many lines change: with only a paper you must first invent it and then spend a training run discovering the invention was wrong. `code_availability` is therefore recorded beside V, not folded into it.",

  "_comment_invariants": "What `graph.py check` enforces, and what each failure means. It REPORTS; it never repairs — a graph that repairs itself hides the fact that something wrote an illegal state.",

  "updated_at": null
}
